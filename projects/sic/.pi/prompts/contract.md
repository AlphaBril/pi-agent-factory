---
description: Write a Structured Implementation Contract interactively (scribe phase only)
argument-hint: "<what-to-build>"
---
Dispatch the `scribe` agent from the `sic-pipeline` team to write a Structured Implementation Contract for:

$@

Only produce the contract — do not proceed to implementation.
