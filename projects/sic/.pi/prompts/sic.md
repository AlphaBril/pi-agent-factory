---
description: Start the SIC pipeline — the foreman will ask what you're doing today
argument-hint: "[optional initial context]"
---
Dispatch the `foreman` agent from the `sic-pipeline` team to begin a Structured Implementation Contract session.

The foreman will ask "What are we doing today?" and then orchestrate the full pipeline: scribe → scout → mason → inspector → auditor → clerk.

$@
